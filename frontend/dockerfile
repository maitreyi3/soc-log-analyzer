# frontend/Dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Next.js: ensure "start" script exists in package.json:
# "start": "next start -p 3000"
RUN npm run build

EXPOSE 3000
ENV NEXT_TELEMETRY_DISABLED=1
CMD ["npm", "start"]
